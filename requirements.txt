fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
httpx==0.27.0
pytest==8.3.0
# Optional real providers (uncomment as needed):
# anthropic==0.40.0
# google-generativeai==0.8.0
# openai==1.50.0
# Optional vector store — app/vectorstore_chroma.py (lazy import):
# chromadb>=1.0
# Optional LangChain retriever adapter — app/langchain_adapter.py (lazy import):
# langchain-core>=0.3
